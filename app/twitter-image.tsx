// Twitter card image — identical layout to opengraph-image but Twitter uses
// summary_large_image crop. Reusing the same composition keeps the brand
// consistent.

export { default, runtime, alt, size, contentType } from "./opengraph-image";
